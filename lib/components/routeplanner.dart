import 'package:flutter/cupertino.dart';

class routeplaner extends StatelessWidget {
  const routeplaner({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
